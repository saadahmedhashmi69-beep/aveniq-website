/**
 * Only technologies verifiably in use — this website's own stack
 * (package.json) and its stated deployment target. Anything not
 * confirmed (a backend language, a database, cloud infrastructure) is
 * deliberately omitted rather than guessed. See
 * docs/phase-1-architecture.md, section on Technology positioning.
 */
export const technologies: string[] = ["Next.js", "React", "TypeScript", "Tailwind CSS", "Vercel"];
